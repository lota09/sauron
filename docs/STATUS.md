# sauron_reborn 진행 현황

_기준(2026-09-22): 오프라인 테스트 130/130 통과. "온디바이스 초저비용·무외부의존·누락0" 철학._
_확장(나중에 할 것: API·RSS·AI에이전트봇) 평가는 [`ROADMAP.md`](ROADMAP.md)._

---

## ✅ 완료 (built & 오프라인 검증)

**2026-09 추가 (운영 반영 완료)**
- 진로취업센터 3곳(기업채용공고·진로취업프로그램·공모전·기타) + 슈패스 비교과 — 모두 `기타` 카테고리·구독 ③단계.
- 플러그인 시스템(`core/plugin.py`, `plugins/`): 로그인 필요 사이트는 플러그인 한 파일 + CSV 한 행 + `deploy.py`. 슈패스가 첫 플러그인(로그인·학년도 3개 조회·잠금 방지).
- 사이트 전용 코드 제거 → 범용 옵션(fetch_config)만. `seed_db`가 CSV를 먼저 검증. README '새 학교에 도입'.
- 수집 실패 알림은 상태가 바뀔 때만(처음·원인변경·복구, 6시간 리마인드). 오류는 원인 타입 + 우리 코드의 파일:줄.
- 감시채널 연결 복구(app_meta) + `setup_guild` 저장 검증(`--check`) + deploy가 동기화 누락·이름변경·이동을 감지.
- `/상태`: 크롤러 + LLM 서버 + 최근 요약 성공/실패를 키-값으로(판정 문구 없음). 3초 규칙(defer) 적용.
- 카테고리 '공통 공지' → '홍보센터'(설정 `GENERAL_CATEGORY_NAME`, 제자리 이름 변경).
- LLM: 읽기 타임아웃 20→60초(첫 토큰 대기 포함 — 긴 입력+이미지 prefill이 20초를 넘겨 잘리던 것), 시간초과/끊김/스트림 속 서버오류 구분 로그.
- 이미지: 이미지가 아니면(로그인 페이지 HTML 등) 보내지 않음, 플러그인 학과는 그 로그인 세션으로 받음.
- OCR·Clova 폐기(검증 불가한 레거시).

**코어 파이프라인**
- 설정주도 크롤러: 수집 방식 2개(html·json_api) + fetch_config 범용 옵션. 사이트 전용 코드 없음(2026-09-22 제거).
- URL 차집합 신규감지 + 최초 시딩(무발송) + UPDATE_LIMIT 대량알림 가드.
- 단일 asyncio: 크롤 → 감지 → D1 즉시발송(제목+링크) → 요약큐 → D2 요약 edit.
- SQLite WAL. schema v4: `seen_notices`를 `notices`로 **단일 테이블화**(status: seeded→detected→notified→done/…). 시딩이 제목까지 기억 → query 검색 가능.

**요약/LLM**
- OpenAI 호환(OlliteRT, Gemma-4-E2B-it) 스트리밍 + 모델 자동감지.
- 결정론 검증기: strip_degenerate(반복붕괴)·REFUSAL_PATTERNS(거절)·language_issue(외국문자).
- 사유별 재시도: 모델오류→폴백(무차감)/연결→대기재시도/검증실패→리롤(프롬프트 미세변형, 실측 입증). 공지당 1회, 소진 시 영구실패.
- 실패사유 DB 기록(fail_reason). 성공요약 면책문구·실패문구·내용없음문구.
- **멀티모달**: 공지에 이미지 있으면 무조건 첨부(텍스트 프롬프트 공유, 최대 4장, 다운스케일). 온디바이스 비전 동작·성능 실측(`vision_llm_experiment.md`).

**디스코드**
- 구독봇 3단계(홍보센터→전공→기타) + 공개 상주버튼(재시작 후에도 동작) + 관리자 `/구독버튼생성` + 임베드 + 로깅.
- `setup_guild`(이름 기준 실존확인) — 역할/채널 63개 생성·재사용 + **감시(디버그)채널 자동 생성**(app_meta에 ID 저장).
- 발송 라우팅: `--dst` 단일 축 — `null`(무발송)·`mono`(통합채널)·`poly`(학과채널)·`<채널ID>`. 멘션은 보내지 않음.
- poly인데 학과채널 미배정 시 통합채널 폴백 + **경고 로그**(조용한 오배송 방지). 감시채널은 config `DEBUG_CHANNEL_ID` 또는 setup_guild 자동생성분(app_meta).
- 첫 세팅 필수값 = **봇 토큰 + `DISCORD_GUILD_ID` 둘뿐**(나머지 채널ID는 setup_guild가 채움).

**CLI/라우팅**
- 모드: `run`(상시) · `once`(1회, cron 근사) · `redo N`(임의 재처리 — `--depts`·`--exclude`·`--per`·`--random`) · `query "검색어"`(검색→선택→재처리·삭제) · `check`(학과별 수집 점검, 발송·DB 기록 없음).
- `--dst {null|mono|poly|<채널ID>}`(택1, 기본 null) + `--nosummary`(직교: 요약·상세fetch 생략, dst null과 함께면 순수 시딩).

**DB 헬퍼**: forget_url · search_notices · delete_notice · get_meta/set_meta(app_meta).

**이미지**
- **다중 이미지 추출 버그 수정** — `_img_base`가 워드프레스 해상도 접미사(`-1568x2216`)뿐 아니라 `_숫자`(붙임 `_1/_2/_3`)까지 지워 3장짜리 공지가 1장으로 뭉개지던 것 수정. 회귀테스트 `test_image_multi_extract` 추가.
- 이미지 입력 로그: `[요약 완료] … (이미지 N/M장 입력)`(전송/추출), 로드 실패 시 `[이미지 제외]`, 상한 초과 시 `[이미지 상한]`.

**문서**: DESIGN.md · ROADMAP.md · vision_llm_experiment.md · embed_gallery.html · README · STATUS(본 문서).

---

## 🔵 지금 검증할 차례

1. **다중이미지 수정 실검증** — `query '군e러닝' --dst mono` → 재처리 → `vision:…×3` + 로그 `(이미지 3/3장 입력)` 확인(수정 전엔 ×1이었음).
2. **결정론 진단** — 같은 입력에 다른 요약 관측. `LLM_TEMPERATURE=0`으로 놓고 재현되는지 확인(서버 기본 샘플링이 비-greedy인지 판별). 비전 경로 GPU 비결정성/조용한 리롤 가능성도 함께 염두.
3. **감시채널 자동생성 검증** — `python -m notify.setup_guild` 재실행 → `사우론-감시` 채널 생성/재사용 + app_meta 저장 → 오류 시 디버그 임베드 도착 확인.
4. **이미지 추출 사이트별 감사** — scatch 확인됨. disu(`#printbody > div`) 재확인, 나머지 학과 표본 점검.
5. **전체 E2E(디바이스)** — 시딩 → `run --dst poly`(setup_guild 선행) → 실LLM → 발송 → edit 한 바퀴.

---

## ⬜ 앞으로 (우선순위 순)

**A. 설정·ID 위치 정리** — `config.default.toml`(git) + `config.toml`(배포별) 통합, `secrets/discord.json`.
  스키마 v6 `guild_resources`: 디스코드 채널·역할 ID를 `depts`·`app_meta`에서 분리(CSV에서 서버 채널ID 제거, 서버 전환 시 ID 섞임 방지), `ocr_text` 열 정리.

**B. 결정 대기** — 같은 URL에서 제목만 바뀐 글(제자리 수정)을 '수정됨'으로 알릴지 · 실서비스 서버 전환(`PROD_GUILD_ID`) 여부 · `experiments/ssupath_probe.py` 유지 여부.

**C. 요약 품질** — 긴 본문+이미지는 폰에서 벽시계 상한(120s)에 걸려 반복붕괴 → 잘린 짧은 요약이 '완료'로 저장되는 경우가 있음(예: 선배멘토링 50자). 2B 모델의 숫자 오독(날짜·전화번호). → 입력 축소 규칙 또는 모델 교체(에이전트 트랙과 연동).

**D. 확장** — 코엑스 등 행사 일정(URL은 정규식 옵션으로 해결, 남은 건 관련성 필터 + 요약 생략 범용 옵션) · 링커리어·자소설닷컴(공개 JSON API)·공공 OpenAPI → 개인 맞춤 에이전트([`AGENT.md`](AGENT.md)) 1단계 '수집만' 모드. Notifier 플러그인은 보류.

---

## ⚠️ 알려진 이슈/미결
- 비전 정확도는 2B 한계(날짜·수치 오독 가능) → 면책문구로 보완, 정밀정보는 원문 링크.
- 이미지 항상첨부 → 이미지 있는 공지는 느림(첫 토큰 10~25초). 의도된 트레이드오프, 읽기 타임아웃 60초로 수용.
- 제자리 수정된 글은 URL이 같아 신규로 잡지 않음(국어국문학과 1학기→2학기 사례). 위 B.
- 요약 실패율 추적: 2026-09 초 2주간 72%(`[conn]` 대부분). 타임아웃 수정(09-22) 이후 추이를 `/상태`로 확인할 것.

# ── 런타임 핵심 (무의존 철학: 최소) ──
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# ── OCR (선택; 타겟 기기에서만 설치 · RAM 주의) ──
# Tesseract 백엔드(가벼움): 시스템 tesseract-ocr, tesseract-ocr-kor + 아래
#   pytesseract>=0.3.10
#   Pillow>=10.0
# PaddleOCR 백엔드(정확·무거움; 온디맨드 로드 권장):
#   paddleocr
#   Pillow>=10.0
#   numpy

# ── 디스코드 구독 봇 (notify/discord_bot.py, setup_guild.py 실행 시) ──
# discord.py>=2.3

# 표준 asyncio 사용(aiohttp/openai/apscheduler 불필요). 요약 LLM은 OpenAI 호환 REST를 requests로 직접 호출.
# 크롤/요약 파이프라인은 discord.py 없이도 동작(봇만 별도 프로세스).
